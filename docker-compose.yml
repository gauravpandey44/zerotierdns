version: "3.7"
services:
  dns:
    image: gauravpandey44/zerotierdns
    restart: always
    volumes:
      - ./.config:/.config
      - ./dnsmasq.d/:/etc/dnsmasq.d/
    ports:
      - 53:53/udp
    
